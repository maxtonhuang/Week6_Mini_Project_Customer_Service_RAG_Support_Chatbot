# RAGGuard — runtime dependencies (Colab-friendly; Python 3.10-3.12 recommended)
# On Colab most of torch is preinstalled; `pip install -r requirements.txt` tops up the rest.

transformers>=4.44,<5
torch>=2.2
sentence-transformers>=3.0
faiss-cpu>=1.8
datasets>=2.20
accelerate>=0.33
optuna>=3.6
gradio>=4.44
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# --- dev / test only (not needed to run on Colab) ---
pytest>=8.0
