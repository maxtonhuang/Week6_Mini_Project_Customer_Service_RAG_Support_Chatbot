# Build Status & Handoff

Generated by the automated build. Maps the plan's §9 tasks to what is **done** vs what
**the team must run on Colab / write themselves**.

## ✅ Built & tested (offline, in this repo)

**80/80 automated tests pass** (`python run_tests.py`). All logic is verified with
stdlib-only test-doubles because the models run on Colab, not here.

| Plan task | Status | Where |
|---|---|---|
| T0 Setup, config, dials | ✅ | `ragguard/config.py`, `requirements.txt`, `pyproject.toml` |
| T1 Victim system | ✅ | `prompts.py`, `canary.py`, `corpus.py`, `rag.py` |
| T2 Eval harness (judge, metrics) | ✅ | `judge.py`, `metrics.py`, `detect.py` |
| T3 Static attacks A1–A6 | ✅ | `attacks/static.py` |
| T4 Adaptive attacker A7 | ✅ | `attacks/adaptive.py` |
| T5 Defenses D1–D6 | ✅ | `defenses/impl.py` |
| T6 Orchestration & 2-stage search | ✅ | `orchestrator.py` |
| T7 Governance scorecards | ✅ | `governance.py` |
| T8 Gradio UI (4 tabs) | ✅ code | `app.py` (+ tested `DemoController`) |
| Report plots/tables | ✅ | `report.py` |
| Notebooks | ✅ | `00_MAIN.ipynb`, `01_DEMO.ipynb` |

**Offline sanity run** (ScriptedLLM doubles): undefended ASR **87%** → full-stack **0%**.

## ✅ FULL run complete — Qwen3-8B on an RTX 5090 (32GB)

The **entire pipeline** ran for real (all 6 defences, **all 64 stack subsets**, N=50, full
150-q benign for finalists) in **15.3 minutes** — the optimisations (batched generation +
disk cache at **89% hit rate** + benign-for-finalists) turned the naive ~8 h run into 15 min
with identical results. Checkpointed per phase (`artifacts/full/`), resumable.

| Attack | Undefended → Full-stack (real) |
|---|---|
| A1 direct injection | **88% → 0%** |
| A2 jailbreak | 26% → 0% |
| A3 indirect injection | 8% → 0% |
| A4 system-prompt extraction | **0% → 0%** (aligned model never leaks it) |
| A5 canary extraction | 24% → 0% |
| A6 obfuscated injection | 4% → 0% |
| **Overall** | **25% → 0%** |

- **Best stack `[D4+D5]`** (output filter + groundedness) → robustness **100%**, utility
  **0.45**, **FRR 0%**. The full 64-stack search found a different minimal stack than the
  earlier bounded run ([D2+D3]) — both reach 100% robustness at 0% FRR.
- Adaptive attacker: **42%** flat across 6 rounds (a real finding; an LLM-driven attacker
  would likely do better — noted for the report).
- **Gradio UI** (redesigned via the ui-ux-pro-max skill): status-strip of key metrics,
  numbered ①②③ step-cards with semantic red (attack) / green (defence) borders, Fira/system
  typography, and the Attack Lab table now showing **ID · Attack name · Type · ASR**.
  Live tools: per-query demo, quick attack sweep, live stack-evaluator.
- Screenshots in `artifacts/`: **use the `ui_v2_*.png` set** (current redesign) —
  `ui_v2_livedemo`, `ui_v2_livedemo_attack` (🔴 succeeded), `ui_v2_attacklab` (named table),
  `ui_v2_defenselab`, `ui_v2_governance`. (The older `ui_*.png` are the pre-redesign layout.)

Why this beats the brief's 0.5B: Qwen3-8B has genuine safety alignment, so ASRs are
**realistic and differentiated** (25%, not a saturated 87%) — a much stronger case study.

### Environment (already set up on this machine)
`.venv/` with PyTorch **2.11+cu128**, transformers, sentence-transformers, faiss, gradio,
optuna, etc. Re-run locally with:
```
.venv/Scripts/python run_real.py      # bounded real pipeline -> artifacts/
.venv/Scripts/python serve_app.py     # launches the Gradio UI on http://127.0.0.1:7860
```

## ⬜ What the team must do

1. **Colab smoke test (do first).** Upload this folder, open `00_MAIN.ipynb`, GPU=L4,
   run the setup + §1 cells. Confirm Qwen loads and answers one benign question.
2. **Full run.** `FAST=True` first (~8 min) to validate end-to-end, then `FAST=False`
   for the report numbers. Artefacts land in `artifacts/` (or Drive).
3. **Launch the UI.** `01_DEMO.ipynb` → `launch(share=True)`. Rehearse the 4-beat demo
   on Tab 1 (or the "Run demo script" button).
4. **Write the report** (§1–4). Prose must be team-written (academic integrity). All
   the numbers/plots come from `00_MAIN.ipynb` artefacts.
5. **Slides** + **screen-record a backup demo video**.

## How to run the tests
```
python run_tests.py            # all 80, stdlib only (this sandbox)
python run_tests.py test_integration   # the end-to-end wiring proof
# On Colab you can also: pytest -q
```

## Attack ↔ defense reference

| Attack | Countered by |
|---|---|
| A1 direct injection | D2 (guardrail), D1 (spotlighting) |
| A2 jailbreak | D2 |
| A3 indirect injection (poisoning) | D3 (retrieval sanitisation), D1 |
| A4 system-prompt extraction | D4 (output filter) |
| A5 canary extraction | D4 |
| A6 obfuscated injection | D6 (normalise) → then D2 |
| A7 adaptive | the selected stack (measured, not assumed) |
